"import json\n\nlog_path = \"/Users/avijaybabu/.gemini/antigravity-ide/brain/94be2d3e-d944-4ac3-b86b-842bfd1e7f6c/.system_generated/logs/transcript.jsonl\"\n\nprint(\"Parsing log file to extract ProblemsPage.tsx...\")\n\nlongest_content = \"\"\n\nwith open(log_path, 'r', encoding='utf-8') as f:\n    for line in f:\n        try:\n            # We can search the raw line string first for keywords to make it fast\n            if \"interface TopicDetailsResponse\" in line:\n                data = json.loads(line)\n                # Walk through the dictionary values to find any string containing the code\n                def find_code(obj):\n                    nonlocal longest_content\n                    if isinstance(obj, str):\n                        if \"interface TopicDetailsResponse\" in obj and \"export default ProblemsPage;\" in obj:\n                            # Clean up escaping\n                            clean_str = obj\n                            if len(clean_str) > len(longest_content):\n                                longest_content = clean_str\n                    elif isinstance(obj, dict):\n                        for val in obj.values():\n                            find_code(val)\n                    elif isinstance(obj, list):\n                        for item in obj:\n                            find_code(item)\n                \n                find_code(data)\n        except Exception as e:\n            pass\n\nif longest_content:\n    print(f\"Success! Found a version of ProblemsPage.tsx of length {len(longest_content)}\")\n    # Clean up double escapes if any\n    output_path = \"/Users/avijaybabu/Desktop/CodeForge-1/scratch/problems_page_recovered.tsx\"\n    with open(output_path, 'w', encoding='utf-8') as out:\n        out.write(longest_content)\n    print(f\"Saved recovered file to {output_path}\")\nelse:\n    print(\"Could not find a matching ProblemsPage.tsx in logs.\")\n"