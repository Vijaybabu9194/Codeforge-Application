server:
  port: 8080

spring:
  application:
    name: codeforge-backend
  
  datasource:
    url: jdbc:postgresql://ep-bitter-bonus-aqt125cn.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require&reWriteBatchedInserts=true
    username: neondb_owner
    password: npg_nav3E9wLyMUH
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 50
          lob:
            non_contextual_creation: true
        order_inserts: true
        order_updates: true
  
  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

  # Physical Mail Dispatched via SMTP
  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:codeforge.verify@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:demo-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: codeforge-super-secret-key-that-is-at-least-256-bits-long-for-hs256-algorithm
    expiration-ms: 86400000 # 24 hours
  judge0:
    url: https://ce.judge0.com

logging:
  level:
    com.codeforge: DEBUG
    org.springframework.security: WARN
